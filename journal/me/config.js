/* Настройка кабинета для родителей.
 *
 * Впишите между кавычками адрес функции синхронизации — тот же, что
 * стоит в журнале в разделе «Ещё» (docs/JOURNAL.md, шаг 3.3).
 *
 * Правьте только этот файл: при обновлении приложения index.html
 * заменяется целиком, а config.js остаётся — адрес не потеряется.
 */
window.JOURNAL_API = "";
